path: '/workspace/wordpress'
url: 'http://local.divi-dev.site'
user: divi-dev
debug: false
allow-root: true
core config:
  dbhost: 127.0.0.1
  dbname: wordpress
  dbuser: root
  dbpass: password
  extra-php: |
    define( 'WP_DEBUG', true );
    define( 'WP_DEBUG_LOG', true );
